// Portfolio routes — implemented in Phase 3
//
// Planned endpoints:
//   GET  /api/v1/portfolio          → Fetch published items (public)
//   POST /api/v1/admin/portfolio    → Create item with image upload (admin)
//   PUT  /api/v1/admin/portfolio/:id
//   DELETE /api/v1/admin/portfolio/:id

const router = require('express').Router();

module.exports = router;
